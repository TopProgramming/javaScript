npm init -y //Iniciamos proyecto json
npm run index.js // Correr en terminal el archivo

en el package.json

en scripts se agrega
"start" : "index.js" 

declaracion de objetos 

const nameOnj = {
    atributos 
    atr1 : valor ;
}

acceso nameOnj.atr1